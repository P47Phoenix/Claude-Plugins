# Plan Stage Lessons

**Entries**: 7 | **Last updated**: 2026-04-08

- Sprint plan MUST include capacity declaration (velocity baseline, 80% ceiling, per-sprint commitment %) before SM validation. SM will reject without it. (validated: 2, last: run-2026-03-29-h3k7)
- Test cases must cover ALL PRD functional requirements explicitly. QA validator checks FR-by-FR traceability. FR-14 (mutual exclusion) and FR-15 (default fallback) were missed on first pass. (validated: 1, last: run-2026-03-27-c8f2)
- Markdown-only edits should use calibrated estimates one tier lower than code changes. 3L+1M+1S capacity plan for markdown work was overcommitted; calibrated to 3M+2S after SM flagged >80% ceiling breach. (validated: 1, last: run-2026-03-29-h3k7)
- Adversarial review at Plan stage catches sprint overloading that SM correction alone misses. In run r4x2, PO proposed 169% ceiling → SM corrected to 3 sprints → Challenger caught Sprint 1 still at 100% and moved US-05. Two correction rounds produced an exact-delivery plan. (validated: 1, last: run-2026-03-30-r4x2)
- Plan stage agents need pre-loaded constraints (sprint ceiling, mandatory artifact list) injected into their prompts — not just the validator prompts. Plan has the lowest first-try pass rate (57%, 4/7 runs). Three of seven runs required rework for constraints the agents already knew. Inject constraints upstream, not just at validation. (validated: 1, last: run-2026-04-02-k3r9)
- Live dogfood of Architect-in-Plan integration (ADR-002) during the build that introduced it caught 3 unpriced gaps (schema forward-compat AC-1.4, cache-refresh AC-9.4, S3 intra-order). The capability is self-validating when invoked mid-build. (validated: 1, last: run-2026-04-08-a1f3)
- Self-correction round 2 triggered when validators reference amendments that exist only in adjacent artifacts (sequencing.md) and not in authoritative artifact (stories.md). Propagate amendments to the AUTHORITATIVE source, not just the referencing doc. (validated: 1, last: run-2026-04-08-a1f3)
- **QA test-strategy validator MUST enumerate ALL initiative-level ACs** (count carefully; don't trust upstream tally). At Wave 3 R1, QA Pippin caught BACKLOG-104 has 10 init ACs (not 7 as test-strategy claimed); 3 ACs unmapped (8/9/10). Same risk class as Refine "PRDs from audit prose MUST run discovery commands". Action: validator dispatch prompt MUST include "count BACKLOG-N initiative ACs from the source file; verify tally". (validated: 1, last: run-2026-05-09-tk4)
