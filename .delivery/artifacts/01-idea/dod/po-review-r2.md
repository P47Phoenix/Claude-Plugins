<!-- run: run-2026-05-05-tk3 | reviewer: PO DoD validator (FRESH dispatch, round 2) | role: product-delivery | round: 2 of 2 (light) -->

STATUS: DONE
ARTIFACT: .delivery/artifacts/01-idea/po/idea-brief.md
PIPELINE_ID: run-2026-05-05-tk3
WAVE: caveman-lite (Step 1 of 4-step Skill Token-Economy completion plan)
ROUND: 2 of 2 (light-mode max, fresh validator)
TIMESTAMP: 2026-05-05

---

# DoD Validation R2: Caveman-Lite Idea-Brief (Stage 1, Light) — Fresh PO Lens

## Findings (per gate criterion)

- **Criterion 1 — Engagement named, brief (≤5 sentences in opener)**: PASS. The opener spans `idea-brief.md:3-15`: a titled H1 (line 3), a one-line speaker tag (line 5), a one-line in-character hook (line 7), and §1 (lines 9-15) — six key/value bullets naming pipeline `run-2026-05-05-tk3`, project type FEATURE, wave caveman-lite, theme lotr, and predecessor `run-2026-05-05-tk2` (c2e7d5a). Bullets serve here as structured prose; total opener payload is well under the 5-sentence intent (the in-character line at `idea-brief.md:7` is the only true sentence, the rest is metadata). Engagement is unambiguous and signals everything a downstream stage needs to route.
- **Criterion 2 — BACKLOG-102 cited by path, NOT duplicated**: PASS. `idea-brief.md:19` cites `.delivery/backlog/BACKLOG-102-caveman-prose-discipline.md` as authoritative and adds the explicit binding statement "this idea brief CONSOLIDATES — it does not re-author. All tier scoping, work-item ACs, and exemption rules live in BACKLOG-102 verbatim; the team executes against BACKLOG-102, not against this brief." Spot-checked §3, §4, §7, §8, §9: each either points back to BACKLOG-102 (`:69`, `:55`, `:86`) or carries the brief's own consolidation decisions (story collapse, cache-prefix call-out) that are in-scope for an idea brief and not duplicated from the backlog. No verbatim ACs or tier scoping copy-paste detected.
- **Criterion 3 — Goal measurable; 5 acceptance gates referenced**: PASS. §3 (`idea-brief.md:28`) states the goal with two quantified thresholds (≥20% prose-token reduction; ≥25% DoD-file size reduction) plus the no-regression and auto-clarity-honored qualitative gates — measurable and falsifiable. The full set of 5 acceptance gates is referenced and carried verbatim at §8 (`idea-brief.md:69-80`) with explicit attribution "verbatim from BACKLOG-102 §Acceptance Criteria" and an additional Stage-7 carry-forward (`idea-brief.md:78`) requiring validator prompts to cite `.delivery/artifacts/<NN>-<stage>/` paths.
- **Criterion 4 — Constraints name (a) 5 binding rulings, (b) plugin-dev:skill-development, (c) FEATURE-execution + binding-decisions-in-memory, (d) Ruling 1 cache-prefix invariant**: PASS on all four sub-points. (a) §5 enumerates all 5 rulings: cache-prefix freeze (`:38`), `disable-model-invocation` boundary (`:39`), Tier-A/B/C line budgets (`:40`), agent prompts as markdown references (`:41`), `allowed-tools` whitelist scope (`:42`). (b) `idea-brief.md:49` binds `plugin-dev:skill-development` as a pre-load at Stage-6 developer dispatch, plus `plugin-dev:skill-reviewer` and `plugin-dev:plugin-validator` post-completion. (c) `idea-brief.md:45` names "FEATURE-execution-of-pre-planned-waves with binding-decisions-in-memory" verbatim and adds the operative directive "execute, do not re-debate". (d) §6 (`idea-brief.md:51-53`) is a dedicated cache-prefix-invariant call-out: identifies W2-1 + W2-3 as the prefix-touching changes, names `governance/cache-prefix-hash.txt` as the freeze artifact, and assigns `ADR-tk3-001` as the re-freeze owner contingent on the Stage-4 architect's confirmation.
- **Criterion 5 — Routing decision recorded with stage-by-stage depth + Stage 3 SKIP rationale**: PASS. §7 (`idea-brief.md:57-65`) provides the seven-row stage table with depth assignments — Idea light, Refine light, Design SKIP, Architect light w/ ADR-tk3-001, Plan light, Development full, UAT full — each with one-line operational notes. The Stage-3 SKIP rationale is broken out at `idea-brief.md:67`: "BACKLOG-102 changes prompt-template strings and a config key. There is no user-facing UI surface, no wireframe, no component spec." It cites the validated precedent run `run-2026-04-22-4x7e` and the anti-fusion guard "skip recorded — not silently fused" — directly honoring the no-skip-stages feedback (light ≠ skip; SKIP must be explicit, justified, and pattern-matched).
- **Criterion 6 — Story consolidation visible (1 story, Effort S, 3 WIs, file-scope rationale)**: PASS. §4 (`idea-brief.md:30-32`) is explicit: collapse to "ONE Story 1 (Effort S)" at Plan stage; names all 3 WIs (W2-1 dispatch templates, W2-2 validator templates, W2-3 `prose_style:` config key); states the file-scope rationale ("all 3 touch overlapping prompt-template surfaces in `delivery-team/skills/delivery-flow/`") and enumerates the touched surfaces (SKILL.md Step 4 + Step 7 + Phase 0 config-read; `references/pipeline-stages.md`, `references/quality-gates.md`, `references/config-schema.md` v2.7→v2.8, regenerated `config-schema.json`); cites the controlling memory pattern (`topics/project-types.md` "Story consolidation by file scope"); and frames the trade as "Same DoD coverage; one developer dispatch instead of three".
- **Criterion 7 — Stop-rule recorded (defects/story >0.4 across 3-PR window)**: PASS. §9 (`idea-brief.md:82-88`) carries the canonical rule verbatim from `skill-token-economy.md` at `idea-brief.md:84` ("defects/story rate >0.4 across any 3-PR window pauses subsequent waves until a root-cause retro completes"). A second engagement-local stop-rule from BACKLOG-102 §Stop-rule is layered (`:86`): pause Tier-2 A/B if <15% prose-token reduction OR if any DoD validator misses a finding due to over-compression. Both are explicitly "armed for this run" (`idea-brief.md:88`) — no hand-wavy language.
- **Criterion 8 — Length 80-150 lines**: PASS. The artifact is 102 lines (`idea-brief.md:1-102`), inside the light-mode envelope with ~22 lines of headroom on each side of the band. Density is appropriate: HTML provenance comment (line 1), 11 numbered sections, no padding sections detected.

## Verdict

All 8 gates pass on a fresh validator read. The brief acts as a directive to downstream stages — consolidating BACKLOG-102 by reference, naming binding rulings as non-negotiable, and pre-resolving the routing/story/cache-prefix decisions so Refine onward can execute without re-debate. TARGET framing is correctly preserved (the brief grades itself, not the implementation); proceed to Refine.

```
STATUS: DONE
ARTIFACT: .delivery/artifacts/01-idea/dod/po-review-r2.md
SUMMARY: R2 fresh PO lens — all 8 gates PASS. 102 lines, BACKLOG-102 cited not duplicated, 5 rulings + plugin-dev + FEATURE-binding + cache-prefix invariant all explicit, Stage-3 SKIP rationale + 1-story consolidation + stop-rule armed.
```
