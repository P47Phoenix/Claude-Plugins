# Delivery state — fixture workspace

This file is a fixture for aggregator parsing tests. The aggregator scrapes
the key-value pairs and the Stories section below for completion counters.

stages_completed: 7
human_checkpoints_passed: 4
defects_logged: 2

## Stories

- [x] S1 — wire pipeline
- [x] S2 — baseline + prompt
- [x] S3 — meta-tests + README + Makefile

## Stage history

(Not parsed by the aggregator — kept for visual fidelity to a real workspace.)
